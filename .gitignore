# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Backend (003-auth-persistence)
server/dist
# Compiled output next to the TypeScript it came from. Nothing builds the
# backend tests — `tsconfig.json` is `noEmit` and the build config only
# covers `src` — but a stray `tsc` invocation used to leave these behind,
# and they were committed.
server/test/*.js
server/test/*.js.map
server/test/*.d.ts
/data/
.env
.env.*
!.env.example

# Playwright
test-results/
playwright-report/
playwright/.auth/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
